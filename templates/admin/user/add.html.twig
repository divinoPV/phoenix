{% extends 'base.html.twig' %}

{% block title %}{{ 'admin.user.add.title'|trans }}{% endblock title %}

{% block main %}
    <div class="x-margin">
        {% include 'app/common/button/_back.html.twig' with {path_back: path('admin_user')} only %}
    </div>
    <div class="o-container">
        <h1 class="o-title x-noMargin">{{ 'admin.user.add.title'|trans }}</h1>
        {{ form(form) }}
    </div>
{% endblock main %}
